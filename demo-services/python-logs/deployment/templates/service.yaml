---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.app }}
spec:
  ports:
{{ toYaml (.Values.service_ports) | indent 4 }}
  selector:
    app: {{ .Values.app }}
  type: ClusterIP
  clusterIP: None
